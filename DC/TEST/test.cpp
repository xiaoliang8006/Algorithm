#include <iostream>
using namespace std;


int main()
{
    int l=0;
	int r=3;
	double m=(l+r)/2.0;
	cout <<"answer: "<<m<<endl;
}

if(x[t]<middle){
				while(x[k]<middle) k++;
				Dist=distance(x[t],y[t],x[k],y[k]);
				if(Dist<delta) delta=Dist;
			}else{
				if(x[k]>middle) break;
				Dist=distance(x[t],y[t],x[k],y[k]);
				if(Dist<delta) delta=Dist;
			}
		}

		"minimum distance: "<<